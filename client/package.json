{
  "name": "fleetpro-web",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client"
  ],
  "engines": {
    "node": ">=18 <23"
  },
  "scripts": {
  "build": "cd client && npm run build",
  "start": "npx serve -s client/dist -l $PORT",
  "preview": "npx serve -s client/dist -l $PORT"
  }
}
